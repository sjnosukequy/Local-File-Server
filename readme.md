# Local File Server

A lightweight local file server that lets you transfer files easily, optimize images to WebP format, and browse stored files through a built-in directory index.

---


## 📁 Use Cases
- Upload files from mobile to your pc, laptop,...

- Upload files from pc, laptop,... then access them by phone by file directory

## ✨ Features

- 📁 **File transfer**
  - Upload and download files locally through the server
- 🖼️ **Image optimization**
  - Automatically convert images to **WebP** for better performance and smaller file size
- 📂 **File directory index**
  - Browse all stored files via a web interface at:
    ```
    /files
    ```

---

## 🚀 Getting Started

### Prerequisites
- Node.js (or relevant runtime for your project)
- Local network access (for sharing files across devices)

### Installation

```bash
git clone <your-repo-url>
cd <project-folder>
npm install
node app.js
```